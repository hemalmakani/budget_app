# Exclude Vercel API folder from EAS builds
# These are serverless functions that run on Vercel, not in the mobile app
api/

# Exclude server-only library files
lib/auth-server.ts

# Lambda folders (AWS serverless functions)
lambda/
lambda-recurring/

